pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
# Install NPM dependencies
npm install            }
        }
        // stage('Test') {
        //     steps {
        //         //
        //     }
        // }
        // stage('Deploy') {
        //     steps {
        //         //
        //     }
        // }
    }
}
